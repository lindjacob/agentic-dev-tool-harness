# {{project.name}}

{{project.description}}

> Replace this overview with what an agent needs to know up front: the problem this repo solves and
> the value it delivers (see the `write-docs` skill — "Explain the product, not just the mechanics").

## Documentation index

{{#each docs.areas}}- [`{{this}}/`]({{this}}/)
{{/each}}- [`decisions/`](decisions/) — Architecture Decision Records (ADRs)

## Open work

Tracked as issues in the project's tracker.
